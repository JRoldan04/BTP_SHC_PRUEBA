COLUMN TABLE "T_EMPLOYEE" (
ID_EMPLEADO INTEGER GENERATED BY DEFAULT AS IDENTITY,
NOMBRE          VARCHAR(100),
DEPARTAMENTO    VARCHAR(100),
SALARIO         DECIMAL(10,2),   
PRIMARY KEY (ID_EMPLEADO)
);
